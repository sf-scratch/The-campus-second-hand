<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lh.springboot03.mapper.UserMapper">

    <resultMap id="UserEntityMap" type="com.lh.springboot03.bean.User">
        <id column="uid" property="uid"/>
        <result column="is_delete" property="isDelete"/>
        <result column="created_user" property="createdUser"/>
        <result column="created_time" property="createdTime"/>
        <result column="modified_user" property="modifiedUser"/>
        <result column="modified_time" property="modifiedTime"/>
    </resultMap>

    <!-- 插入用户数据：Integer insert(User user) -->
<!--    <insert id="insert" useGeneratedKeys="true" keyProperty="uid">-->
<!--        INSERT INTO-->
<!--            t_user (username, password, salt, phone, email, gender, avatar, is_delete, created_user, created_time, modified_user, modified_time)-->
<!--        VALUES-->
<!--            (#{username}, #{password}, #{salt}, #{phone}, #{email}, #{gender}, #{avatar}, #{isDelete}, #{createdUser}, #{createdTime}, #{modifiedUser}, #{modifiedTime})-->
<!--    </insert>-->

    <!-- 根据用户名查询用户数据：User findByUsername(String username) -->
    <select id="findByUsername" resultMap="UserEntityMap">
        SELECT
            *
        FROM
            t_user
        WHERE
            username = #{username}
    </select>

    <!-- 根据用户名查询用户数据：User findByUsername(String username) -->
    <select id="findByUid" resultMap="UserEntityMap">
        SELECT
            *
        FROM
            t_user
        WHERE
            uid = #{uid}
    </select>

<!--    查询邮箱是否已注册-->
    <select id="verifyEmail" resultType="java.lang.Integer">
        SELECT
            COUNT(*)
        FROM
            t_user
        WHERE
            email = #{email}
    </select>

<!--    根据uid更新用户密码-->
    <update id="updatePasswordByUid">
        update
        t_user
        set
        password = #{password},
        modified_user = #{modifiedUser},
        modified_time = #{modifiedTime}
        where
        uid = #{uid}
    </update>

    <update id="updateAvatarByUid">
        update
        t_user
        set
        avatar = #{avatar},
        modified_user = #{modifiedUser},
        modified_time = #{modifiedTime}
        where
        uid = #{uid}
    </update>

    <update id="updateInfoByUsername">
        update
        t_user
        set
        phone = #{phone},
        qq = #{qq},
        message = #{message},
        gender = #{gender},
        modified_user = #{username},
        modified_time = #{modifiedTime}
        where
        username = #{username}
    </update>

    <update id="updateModified">
        update
        t_user
        set
        modified_user = #{modifiedUser},
        modified_time = #{modifiedTime}
        where
        uid = #{uid}
    </update>

    <update id="updateEmailByUid">
        update
        t_user
        set
        email = #{email}
        where
        uid = #{uid}
    </update>


<!--    &lt;!&ndash; 根据uid更新用户的密码：-->
<!--	     Integer updatePasswordByUid(-->
<!--		    @Param("uid") Integer uid,-->
<!--		    @Param("password") String password,-->
<!--		    @Param("modifiedUser") String modifiedUser,-->
<!--		    @Param("modifiedTime") Date modifiedTime) &ndash;&gt;-->
<!--    <update id="updatePasswordByUid">-->
<!--        UPDATE-->
<!--            t_user-->
<!--        SET-->
<!--            password = #{password},-->
<!--            modified_user = #{modifiedUser},-->
<!--            modified_time = #{modifiedTime}-->
<!--        WHERE-->
<!--            uid = #{uid}-->
<!--    </update>-->

<!--    &lt;!&ndash; 根据用户id查询用户数据：User findByUid(Integer uid) &ndash;&gt;-->
<!--    <select id="findByUid" resultMap="UserEntityMap">-->
<!--        SELECT-->
<!--            *-->
<!--        FROM-->
<!--            t_user-->
<!--        WHERE-->
<!--            uid = #{uid}-->
<!--    </select>-->

<!--    &lt;!&ndash; 根据uid更新用户个人资料：Integer updateInfoByUid(User user) &ndash;&gt;-->
<!--    <update id="updateInfoByUid">-->
<!--        UPDATE-->
<!--        t_user-->
<!--        SET-->
<!--        <if test="phone != null">phone = #{phone},</if>-->
<!--        <if test="email != null">email = #{email},</if>-->
<!--        <if test="gender != null">gender = #{gender},</if>-->
<!--        modified_user = #{modifiedUser},-->
<!--        modified_time = #{modifiedTime}-->
<!--        WHERE-->
<!--        uid = #{uid}-->
<!--    </update>-->

<!--    &lt;!&ndash; 根据uid更新用户的头像-->
<!--	     Integer updateAvatarByUid(-->
<!--		    @Param("uid") Integer uid,-->
<!--		    @Param("avatar") String avatar,-->
<!--		    @Param("modifiedUser") String modifiedUser,-->
<!--		    @Param("modifiedTime") Date modifiedTime) &ndash;&gt;-->
<!--    <update id="updateAvatarByUid">-->
<!--        UPDATE-->
<!--            t_user-->
<!--        SET-->
<!--            avatar = #{avatar},-->
<!--            modified_user = #{modifiedUser},-->
<!--            modified_time = #{modifiedTime}-->
<!--        WHERE-->
<!--            uid = #{uid}-->
<!--    </update>-->
</mapper>
