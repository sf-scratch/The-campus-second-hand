<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lh.springboot03.mapper.Admin_UserMapper">
<!--    <select id="getCurrentMouthRegisterUser" resultType="Integer">-->
<!--        SELECT COUNT(*)-->
<!--        FROM-->
<!--        t_user-->
<!--        WHERE-->
<!--        DATEDIFF(created_time,DATE_ADD(CURDATE(),INTERVAL - DAY(CURDATE())+1 DAY)) < DAYOFMONTH(NOW())-->
<!--        AND-->
<!--        DATEDIFF(created_time,DATE_ADD(CURDATE(),INTERVAL - DAY(CURDATE())+1 DAY)) + 1 > 0;-->
<!--    </select>-->
</mapper>
